[project]
name = "repo-analyzer"
version = "0.1.0"
description = "Deterministic P0 Kubernetes-migration analyzer for application source repositories"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.6",
    "ruamel.yaml>=0.18",
]

[project.scripts]
repo-analyzer = "repo_analyzer.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/repo_analyzer"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
